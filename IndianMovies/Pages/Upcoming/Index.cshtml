@page
@model IndianMovies.Pages.Upcoming.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UpcomingMovies[0].Movie_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpcomingMovies[0].Movie_Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpcomingMovies[0].Release_Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UpcomingMovies[0].Movie_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UpcomingMovies) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Movie_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Release_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie_ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UpcomingMovies_Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UpcomingMovies_Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UpcomingMovies_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
